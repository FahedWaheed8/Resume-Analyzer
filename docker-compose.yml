version: '3.8'

services:
  resume-analyzer:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - /tmp/uploads:/tmp/uploads
    restart: unless-stopped
